var fs = require('fs');
var webidl = require('webidl2');

function parse(idl_file){
  var idl_fragment_contents = fs.readFileSync(idl_file, 'utf8');
  var parsed_data = webidl.parse(idl_fragment_contents);
  return parsed_data;
}

module.exports = {
	parse: parse
};
